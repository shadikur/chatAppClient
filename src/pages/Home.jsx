import React from 'react';

const Home = () => {
    return (
        <div>
            <h3>This is a simple Chat Application using React, Express and Socket IO</h3>
        </div>
    );
};

export default Home;